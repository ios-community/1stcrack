// Package cli provides command-line subcommands for 1stcrack.
//
// Each invocation opens the embedded database, runs one operation through
// the service layer, prints human-readable output, and exits. There is no
// retained terminal state between invocations.
package cli
